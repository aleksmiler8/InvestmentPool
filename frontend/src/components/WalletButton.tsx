import { AppKitButton } from "@reown/appkit/react";

export default function WalletButton() {
  return (
    <div
      style={{
        width: "100%",
      }}
    >
      <AppKitButton />
    </div>
  );
}